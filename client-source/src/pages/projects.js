import React from 'react';
import PageLayout from '../components/Layout';

const PageContent = () => (
  <div>
    <h1>Projects</h1>
  </div>
);

const ProjectPage = () => (
  <PageLayout>
    <PageContent />
  </PageLayout>
);

export default ProjectPage;
